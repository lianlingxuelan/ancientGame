# 第一章主线成长闭环收口：实施计划

> **目标：** 让第一章的关卡推进遵循“第一关起步 → 首次通关解锁下一关 → 获得材料 → 养成 → 再次挑战”的明确规则；保留重复挑战，但不允许越过前置关卡推进进度。

> **边界：** 仅改 Unity 前端数据与结算路由，不改后端、数据库、`Scene_Boot.unity`、战斗伤害公式、资源目录或商业入口。

## 任务 1：建立主线进度规则验证（RED）

**文件：**
- 新增：`tools/verify_mainline_progression_rules.ps1`

**步骤：**
1. 静态断言第一章默认仅开放第一关。
2. 静态断言进度管理器拒绝未解锁关卡的完成请求。
3. 静态断言关卡状态文案明确区分“可挑战 / 已通关 / 暂未解锁”。
4. 执行脚本，确认在现有实现上按预期失败。

## 任务 2：实现顺序解锁和安全推进（GREEN）

**文件：**
- 修改：`ShouyouPrototype/ShouyouPrototype/Assets/_Project/Scripts/Data/LevelProgressManager.cs`

**步骤：**
1. 将第一章的初始开放关卡设为第一关。
2. 在 `CompleteStage` 内部二次校验关卡是否已解锁，避免未来其它 UI 或调用方绕过入口直接越关。
3. 保留已通关关卡的重复挑战资格；重复挑战不推进最高进度。
4. 保持现有 PlayerPrefs 键与后端进度同步入口兼容。

## 任务 3：让结算与末关操作符合真实进度

**文件：**
- 修改：`ShouyouPrototype/ShouyouPrototype/Assets/_Project/Scripts/UI/HomePageRouter.cs`

**步骤：**
1. 结算仅在首次通关时显示“主线进度 +1”和下一关解锁；重复挑战显示奖励结算但不重复声称推进。
2. 第六关胜利时显示“本章完成”，禁用“下一关”按钮，避免把最后一关导回自身伪装成下一关。
3. 下一关入口添加安全校验，避免未解锁状态下错误跳转。
4. 保持奖励实际入账与既有防连点锁定行为不变。

## 任务 4：回归验证与协作交接

**文件：**
- 修改：`tools/verify_mainline_reward_grant.ps1`（仅补充结算语义断言，如有必要）
- 修改：`docs/AI_TASK_LOG.md`（只在末尾追加）
- 修改：`docs/CLAUDE_NEXT_TASKS.md`（只在末尾追加审查请求）

**步骤：**
1. 运行新主线规则验证及主线奖励、钱包、养成、战斗联动回归脚本。
2. 执行 `git diff --check`，确认本轮代码无空白错误。
3. 在日志中诚实记录静态验证与需人工 Play Mode 验证的路径。
4. 给 Cloud/Claude 留下独立的审查任务；不进行 git 提交。
